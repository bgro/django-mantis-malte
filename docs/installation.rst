============
Installation
============

At the command line::

    $ easy_install django-mantis-malte

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mantis-malte
    $ pip install django-mantis-malte
